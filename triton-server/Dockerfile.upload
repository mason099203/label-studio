FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn[standard] python-multipart

COPY upload_server.py .

EXPOSE 8003

CMD ["uvicorn", "upload_server:app", "--host", "0.0.0.0", "--port", "8003"]
