# 測試以及部署

本文件說明 **本 fork** 的兩種啟動方式：

1. **本機測試（Windows + HMR）**：改程式、除錯 UI/API、Training / RQ / Triton 整合。
2. **Docker 部署**：接近生產的完整堆疊（Nginx + uWSGI + PostgreSQL + Redis + RQ + Triton）。

腳本與 Compose 為單一真相來源；細節以 repo 內檔案為準。

---

## 一、前置準備

### 1.1 共通

| 項目 | 版本／工具 |
|------|------------|
| Python | `>=3.10,<4`（建議 **3.12**，與 Dockerfile 一致） |
| Poetry | 根目錄 `poetry install` |
| Node + Yarn | `web/` 內 `yarn install --frozen-lockfile` |
| Docker Desktop | 本機 Redis / Triton / Docker 部署皆需要 |

### 1.2 環境變數（`.env`）

```powershell
# 專案根目錄
Copy-Item .env.example .env
# 依本機修改 TRITON_MODEL_REPOSITORY 等
```

`.env` 已列入 `.gitignore`，請勿提交。主要變數：

| 變數 | 本機 dev | Docker Compose |
|------|----------|----------------|
| `FRONTEND_HMR` | `true` | 不需要（前端已 build 進映像） |
| `FRONTEND_HOSTNAME` | `http://localhost:8010` | — |
| `REDIS_URL` | `redis://localhost:6379/0` | 由 compose 注入 `redis://redis:6379/0` |
| `TRITON_MODEL_REPOSITORY` | 本機絕對路徑或 `./data/triton_models` | 容器內固定 `/label-studio/data/triton_models` |
| `TRITON_SERVER_URL` | `http://localhost:8000` | `http://triton:8000`（服務間） |
| `TRAIN_SERVER_URL` | `http://localhost:8011`（選用） | `http://train-server:8011`（compose 預設） |
| `TRAIN_SERVER_API_KEY` | （空，可選） | 與 train-server 容器一致 |

PowerShell 啟動腳本會讀取 `.env`（`scripts/Import-DotEnv.ps1`）。

### 1.3 首次資料庫遷移（本機 SQLite）

```powershell
$env:DJANGO_DB='sqlite'
$env:DJANGO_SETTINGS_MODULE='core.settings.label_studio'
poetry run python label_studio/manage.py migrate
```

或使用一鍵腳本：`.\start-all.ps1 -Migrate`

---

## 二、本機測試啟動（Windows）

本機模式：**Django :8080** + **前端 HMR :8010** + **SQLite**。瀏覽器請開 **http://localhost:8010**。

### 2.1 `start-full.ps1`（推薦：完整本機 + 區網）

等同於：

```powershell
.\start-all.ps1 -WithQueue -WithTrainServer -LanAccess
```

會啟動：**Redis（Docker）**、**RQ worker**、**Train Server**、**Django :8080**、**前端 HMR :8010**，並綁定 `0.0.0.0` 供區網存取。

> **不想用 Docker？** 改跑 `.\start-full-native.ps1`（僅 UI + Train Server，無 Redis/RQ）。見下方 §2.1.1。

| 誰在連 | 登入／使用網址 |
|--------|----------------|
| 本機瀏覽器 | **http://localhost:8010** |
| 同網段其他電腦 | **http://\<本機區網IP\>:8010**（腳本結束時會印出，例如 `http://10.214.57.66:8010`） |
| 後端 API（除錯） | `http://localhost:8080` 或 `http://<IP>:8080` |
| Train Server 健康檢查 | `http://<IP>:8011/health` |

**前置條件**

1. 已執行 `poetry install`、`web/` 內 `yarn install`。
2. **Docker Desktop 必須在執行**（`start-full` 需要 Redis 容器）。若看到 `dockerDesktopLinuxEngine` 錯誤，請先開啟 Docker Desktop 再重跑。
3. 首次使用 Train Server：`.\scripts\setup-venv-train.ps1`。
4. Windows 防火牆若阻擋，請允許 **8010、8080、8011** 連入。

**Train / Triton 連線（UI 或 `.env`）**

- 請輸入**完整 URL（含埠號）**，僅填 IP 不會自動補埠。
- 範例（本機）：`TRAIN_SERVER_URL=http://localhost:8011`、`TRITON_SERVER_URL=http://localhost:8000`
- 範例（區網他機連你這台）：`http://10.214.57.66:8011`、`http://10.214.57.66:8000`（本機 `start-triton`）或 `:18000`（Docker 對外映射）

#### 2.1.1 無 Docker 本機測試（`start-full-native.ps1`）

不需 Docker Desktop，等同：

```powershell
.\start-all.ps1 -WithTrainServer -LanAccess
# 或
.\start-full-native.ps1
```

| 元件 | 是否需要 Docker |
|------|-----------------|
| Label Studio UI（HMR :8010） | 否 |
| Train Server（:8011，本機 Python） | 否 |
| Redis + RQ 背景佇列 | **是**（`start-full.ps1`）或另裝本機 Redis（見下） |
| Triton（:8000） | 是（`start-triton.ps1`），或連**遠端** Triton 完整 URL |

**僅 UI / API：**

```powershell
.\start-dev.ps1
# 區網：.\start-dev.ps1 -LanAccess
```

**若不用 Docker 但仍要 RQ 本機訓練**：請自行安裝 Redis（例如 [Memurai](https://www.memurai.com/) 或 WSL 內 `redis-server`），確認 `localhost:6379` 可連後：

```powershell
.\start-rqworker.ps1   # 新終端
.\start-dev.ps1 -LanAccess
```

### 2.2 架構

```
┌─────────────────────────────────────────────────────────┐
│  本機 Windows（PowerShell）                                │
├─────────────────────────────────────────────────────────┤
│  start-dev.ps1  →  Django :8080  +  yarn dev:win :8010 │
│  start-redis.ps1 →  Docker ls-redis :6379               │
│  start-rqworker.ps1 →  RQ SimpleWorker (queue: low)     │
│  start-triton.ps1 →  Docker ls-triton :8000             │
│  start-train-server.ps1 →  YOLO Train Server :8011    │
└─────────────────────────────────────────────────────────┘
```

### 2.3 情境 A：一般 UI / API 開發

只需前後端，不需 Training 背景佇列或 Triton：

```powershell
.\start-dev.ps1
```

- 另開視窗啟動 Django 與前端 HMR（若該埠尚未被占用）。
- 開啟：**http://localhost:8010**

### 2.4 情境 B：Training / YOLO 本機訓練（需 RQ）

需 **Redis** 與 **RQ worker**（Windows 使用 `SimpleWorker`，避免 `os.fork` 錯誤）：

```powershell
.\start-redis.ps1
.\start-rqworker.ps1    # 此終端會被 worker 占用
.\start-dev.ps1         # 另開視窗
```

**順序**：Redis → RQ worker → 開發主流程。

### 2.5 情境 C：獨立 Train Server（YOLO 遠端訓練）

若希望 YOLO 訓練在 **獨立節點** 執行（GPU 機、或與 Django 分離的 torch 環境），而非本機 RQ：

```powershell
# 首次
.\scripts\setup-venv-train.ps1

# 啟動 Train Server（:8011）
.\start-train-server.ps1
```

Label Studio 端（`.env` 或 UI Training 頁面）：

```env
TRAIN_SERVER_URL=http://localhost:8011
```

驗證：

```powershell
curl http://localhost:8011/health
```

一鍵含 Train Server：

```powershell
.\start-all.ps1 -WithTrainServer
# 或同時含 RQ + Triton + Train Server
.\start-all.ps1 -WithQueue -WithTriton -WithTrainServer
```

> 未設定 `TRAIN_SERVER_URL` 時，YOLO 任務仍走 **本機 RQ**（需 Redis + worker）。詳見 `docs/source/guide/train_server_deploy.md`。

### 2.6 情境 D：模型部署至 Triton + 推論

需 Triton 與 Label Studio 共用**同一模型目錄**：

1. 在 `.env` 設定 `TRITON_MODEL_REPOSITORY`（與 Triton 容器掛載來源一致）。
2. 啟動 Triton：

```powershell
.\start-triton.ps1
```

3. 依需求啟動 Redis / RQ / dev（見 2.3、2.2）。

驗證 Triton：

```powershell
curl http://localhost:8000/v2/health/ready
curl http://localhost:8000/v2/models
```

### 2.7 一鍵啟動（其他組合）

```powershell
# 完整本機 + 區網（等同 start-full.ps1）
.\start-full.ps1

# 僅前後端
.\start-all.ps1

# 含 Redis + RQ worker（Training）
.\start-all.ps1 -WithQueue

# 含 Redis + RQ + Triton
.\start-all.ps1 -WithQueue -WithTriton

# 含獨立 YOLO Train Server
.\start-all.ps1 -WithTrainServer
.\start-all.ps1 -WithQueue -WithTriton -WithTrainServer

# 啟動前先 migrate
.\start-all.ps1 -Migrate -WithQueue -WithTriton -WithTrainServer
```

### 2.8 本機腳本對照

| 腳本 | 用途 |
|------|------|
| `start-dev.ps1` | Django + 前端 HMR |
| `start-redis.ps1` | Docker 容器 `ls-redis`（:6379） |
| `start-rqworker.ps1` | RQ worker（queue `low`） |
| `start-triton.ps1` | Docker 容器 `ls-triton`（:8000） |
| `start-train-server.ps1` | 獨立 YOLO Train Server（:8011，`.venv-train`） |
| `start-full.ps1` | 完整開發 + 區網（`-WithQueue -WithTrainServer -LanAccess`） |

| `start-all.ps1` | 組合上述腳本（`-LanAccess` 開放區網；`-WithTrainServer` 含 Train Server） |

### 2.9 本機測試常見問題

| 現象 | 處理 |
|------|------|
| Docker / Redis 啟動失敗 | 先啟動 **Docker Desktop**，再執行 `.\start-redis.ps1` 或 `.\start-full.ps1` |
| 區網其他電腦無法開啟 | 用 **http://\<IP\>:8010**（非 8080）；檢查防火牆是否放行 8010 |
| RQ 報 `fork` 相關錯誤 | 使用 `start-rqworker.ps1`（SimpleWorker），勿用預設 fork worker |
| Training 任務不執行 | 確認已 `start-redis.ps1` 且 worker 在跑 |
| Triton 部署後推論失敗 | 確認 `.env` 的 `TRITON_MODEL_REPOSITORY` 與 `start-triton.ps1` 掛載路徑一致 |
| 前端白屏 / 404 | 確認 HMR 在 **8010**，且 `.env` 有 `FRONTEND_HMR=true` |
| 後端 schema 錯誤 | 執行 `.\start-all.ps1 -Migrate` 或手動 `migrate` |
| Train Server 連線失敗 | 確認 `start-train-server.ps1` 在跑；`curl http://localhost:8011/health` |
| 訓練仍走 RQ 非 remote | 確認已設 `TRAIN_SERVER_URL` 或 UI 填 Train Server URL |

### 2.10 Unix / WSL（非主要路徑）

Windows 以外可用 Makefile：

```bash
make migrate-dev
make run-dev          # 後端
make frontend-dev     # 前端 HMR（web/ 目錄）
```

Redis / RQ / Triton 需自行以 Docker 或系統服務啟動，並 export 對應環境變數。

---

## 三、Docker 部署啟動

Docker 模式：**Nginx :8080** → **uWSGI app** + **PostgreSQL** + **Redis** + **RQ worker** + **Triton** + **Train Server**。

### 3.1 架構

```
瀏覽器 :8080
    ↓
 nginx (:8085 對外映射 8080)
    ↓
 app (uWSGI :8000)
    ├── db (PostgreSQL)
    ├── redis
    ├── rqworker
    ├── triton (:18000 對外映射，容器內 :8000)
    └── train-server (:8011)
```

資料與模型目錄：

- `./data` → 應用資料、上傳檔、SQLite 以外持久化（app 與 train-server 共用）
- `./data/triton_models` → Triton 模型倉庫（app 匯出與 triton 共用）
- `./data/training/models/original` → YOLO 預訓練權重（Train Server 快取）
- `./data/training/models/trained` → 訓練產物（best.pt 等）
- `./postgres-data` → PostgreSQL（可透過 `POSTGRES_DATA_DIR` 覆寫）

### 3.2 建置並啟動

```powershell
# 專案根目錄
docker compose up --build
```

背景執行：

```powershell
docker compose up --build -d
```

停止：

```powershell
docker compose down
```

### 3.3 存取位址

| 誰在連 | URL |
|--------|-----|
| 本機瀏覽器 | **http://localhost:8080** |
| 同網段其他電腦 | **http://\<主機區網IP\>:8080**（需在 `.env` 設 `LABEL_STUDIO_HOST=http://<IP>:8080` 後重建／重啟 compose） |
| Triton HTTP（主機） | 依你設定的完整 URL，例如 `http://localhost:18000` |
| Train Server（主機） | 依你設定的完整 URL，例如 `http://localhost:8011` |

**區網存取（與本機 `start-full` 對齊）**

1. 在專案根目錄 `.env` 設定（將 IP 換成實際區網位址）：

```env
LABEL_STUDIO_HOST=http://192.168.1.10:8080
TRAIN_SERVER_URL=http://192.168.1.10:8011
TRITON_SERVER_URL=http://192.168.1.10:18000
```

2. `docker compose up --build -d`（或 `docker-compose.app-only.yml` 僅本體時，外部服務 URL 同樣須**含埠號**）。
3. 其他電腦瀏覽器開啟 `LABEL_STUDIO_HOST` 所示網址。
4. Training／模型部署 UI 若自訂 Train/Triton，亦須填**完整 URL**（UI 不會自動補埠）。

舊表（服務預設埠，僅供對照）：

| 服務 | 常見 URL |
|------|----------|
| Label Studio（經 Nginx） | http://localhost:8080 |
| Triton 健康檢查 | http://localhost:18000/v2/health/ready |
| Train Server 健康檢查 | http://localhost:8011/health |

> 容器內 app / rqworker 連 Triton 請用 `http://triton:8000`、連 Train Server 請用 `http://train-server:8011`，勿用 `localhost`。

### 3.4 首次部署／更新後

映像 entrypoint 會在偵測到 PostgreSQL 時自動執行 `locked_migrate`。若需手動：

```powershell
docker compose run --rm app label-studio-migrate
```

### 3.5 選用：MinIO 本機 S3

```powershell
docker compose -f docker-compose.yml -f docker-compose.minio.yml up --build -d
```

### 3.6 選用：開發用 override

```powershell
Copy-Item docker-compose.override.example.yml docker-compose.override.yml
# 可啟用 INCLUDE_DEV、env_file: .env 等
docker compose up --build
```

或使用 Makefile：

```bash
make docker-dev-setup
make docker-run-dev
```

### 3.7 Triton / Train Server GPU（選用）

編輯 `docker-compose.yml` 中 `triton` 或 `train-server` 服務，取消 `deploy.resources.reservations.devices` 註解，並安裝 [NVIDIA Container Toolkit](https://docs.nvidia.com/datacenter/cloud-native/container-toolkit/install-guide.html)。

僅啟動 Train Server 映像：

```powershell
docker compose up -d train-server
docker compose logs -f train-server
```

### 3.8 Docker 常見問題

| 現象 | 處理 |
|------|------|
| `depends on undefined service triton` | 確認使用本 repo 最新 `docker-compose.yml`（已含 `triton` 服務） |
| 模型部署成功但 Triton 無模型 | 確認 `./data/triton_models` 有內容，且 app 與 triton 皆掛載此目錄 |
| 建置很慢 / 失敗 | 需下載 torch、前端 yarn；可重試 `docker compose build` |
| 背景訓練不跑 | 確認 `rqworker` 容器在跑：`docker compose ps` |
| 埠 8080 被占用 | 修改 `docker-compose.yml` 的 `nginx.ports` 或停止本機 `start-dev.ps1` |

### 3.9 生產環境注意事項

本 repo 的 `docker-compose.yml` 偏向**整合測試／內網部署**，上線前建議：

- 設定 PostgreSQL 強密碼，勿使用 `POSTGRES_HOST_AUTH_METHOD=trust`
- 設定 `LABEL_STUDIO_HOST` 為實際對外 URL
- Nginx 啟用 TLS（`deploy/nginx/certs`）
- 依負載調整 `rqworker` 副本數：`docker compose up -d --scale rqworker=2`
-  secrets 改用 Docker secrets 或 K8s Secret，勿明文寫在 compose

---

## 四、本機 vs Docker 對照

| 項目 | 本機測試 | Docker 部署 |
|------|----------|-------------|
| 入口 URL | http://localhost:8010（HMR） | http://localhost:8080 |
| 資料庫 | SQLite | PostgreSQL |
| 前端 | HMR 即時編譯 | 映像內 production build |
| Redis | 選用 `ls-redis` 容器 | compose `redis` 服務 |
| RQ worker | `start-rqworker.ps1` | `rqworker` 服務 |
| Triton URL | http://localhost:8000 | 容器內 `http://triton:8000`；主機 http://localhost:18000 |
| Train Server URL | http://localhost:8011 | 容器內 `http://train-server:8011`；主機 http://localhost:8011 |
| 模型目錄 | `.env` → `TRITON_MODEL_REPOSITORY` | `./data/triton_models` |
| YOLO 訓練 | 本機 RQ 或 Train Server | compose `train-server` + `TRAIN_SERVER_URL` |
| 適用 | 日常開發、除錯 | 整合測試、演示、類生產部署 |

---

## 五、相關文件

- 代理／開發地圖：`AGENTS.md`
- 前端 HMR：`web/README.md`
- Triton 部署 API 與模型格式：`docs/source/guide/triton_deploy.md`
- Train Server 架構、部署與測試：`docs/source/guide/train_server_deploy.md`
- Train Server 快速參考：`train_server/README.md`
- 上游部署說明：`README.md`、`docs/source/guide/`

---

## 六、快速指令備查

```powershell
# ── 本機 ──
Copy-Item .env.example .env
poetry install
cd web; yarn install --frozen-lockfile; cd ..
.\start-all.ps1 -Migrate -WithQueue -WithTriton -WithTrainServer

# ── Docker ──
docker compose up --build -d
curl http://localhost:8080
curl http://localhost:18000/v2/health/ready
curl http://localhost:8011/health
docker compose logs -f app rqworker train-server
docker compose down
```
